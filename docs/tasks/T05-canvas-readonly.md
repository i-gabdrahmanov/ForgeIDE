# T05 — Канвас: read-only рендер

- **Веха:** M1
- **Зависит от:** T03
- **Спека:** SD §7 (канвас); SDD FR-2.4

## Скоуп

- Node-graph на `Pane`: узел = `Region` со стилизацией CSS по типу шага и статусу,
  ребро = `CubicCurve` по `depends_on`.
- Автолейаут: топологические колонки (упрощённый Sugiyama), минимизация пересечений — базово.
- Зум/пан, выбор плитки → панель деталей (тип, промпт/скрипт read-only, конфиг).
- Бейджи ошибок валидации на плитках (структура ошибок из T03).
- Бейджи validity из реестра плиток (заглушка до T24 — интерфейс готов).

## Вне скоупа

- Редактирование графа (T22), оверлей статусов прогона (T10).

## Приёмка

- [ ] шаблон forgelite рендерится: все шаги, рёбра, типы различимы
- [ ] граф на 100+ шагов рендерится и панируется без видимых лагов (NFR-2)
- [ ] невалидный pipeline.yaml → бейджи на проблемных плитках с текстом ошибки
